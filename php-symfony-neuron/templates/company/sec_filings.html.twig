{% extends 'base.html.twig' %}

{% block title %}{{ company.name }} - SEC Filings{% endblock %}

{% block body %}
    <div class="container mt-4">
        <h1>{{ company.name }} ({{ company.tickerSymbol }}) - SEC Filings</h1>

        <nav aria-label="breadcrumb" class="mb-4">
            <ol class="breadcrumb">
                <li class="breadcrumb-item"><a href="{{ path('company_show', {'id': company.id}) }}">Company Profile</a></li>
                <li class="breadcrumb-item"><a href="{{ path('company_show', {'id': company.id}) }}">Company Overview</a></li>
                <li class="breadcrumb-item active" aria-current="page">SEC Filings</li>
            </ol>
        </nav>

        <p>This page will display SEC filings for {{ company.name }}.</p>
    </div>
{% endblock %}
