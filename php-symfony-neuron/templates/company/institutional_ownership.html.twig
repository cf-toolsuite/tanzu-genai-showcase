{% extends 'base.html.twig' %}

{% block title %}{{ company.name }} - Institutional Ownership{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <style>
        .metrics-card {
            margin-bottom: 1.5rem;
            border-radius: 8px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }
        .metrics-card .card-header {
            background-color: #f8f9fa;
            font-weight: 600;
            border-bottom: 1px solid #e9ecef;
        }
        .institution-name {
            font-weight: 500;
        }
        .change-positive {
            color: #198754;
        }
        .change-negative {
            color: #dc3545;
        }
        .change-neutral {
            color: #6c757d;
        }
        .metrics-nav {
            margin-bottom: 1.5rem;
        }
        .metrics-nav .nav-link {
            border-radius: 0.5rem;
            padding: 0.75rem 1rem;
            margin-right: 0.5rem;
            font-weight: 500;
            color: #495057;
            background-color: #f8f9fa;
            border: 1px solid #e9ecef;
            transition: all 0.2s ease-in-out;
        }
        .metrics-nav .nav-link:hover {
            background-color: #e9ecef;
            border-color: #dee2e6;
        }
        .metrics-nav .nav-link.active {
            color: #fff;
            background-color: #0d6efd;
            border-color: #0d6efd;
        }
        .metrics-nav .nav-link i {
            margin-right: 0.5rem;
        }
        .ownership-chart {
            height: 300px;
            width: 100%;
        }
        .key-figure-card {
            text-align: center;
            padding: 1.5rem;
            border-radius: 8px;
            background-color: #f8f9fa;
            margin-bottom: 1.5rem;
        }
        .key-figure-value {
            font-size: 2rem;
            font-weight: 700;
            margin-bottom: 0.5rem;
        }
        .key-figure-label {
            color: #6c757d;
            font-size: 0.875rem;
        }
        .institution-profile {
            border-radius: 8px;
            background-color: #f8f9fa;
            padding: 1.5rem;
            margin-bottom: 1.5rem;
            transition: all 0.2s ease-in-out;
        }
        .institution-profile:hover {
            background-color: #e9ecef;
        }
        .institution-profile-header {
            display: flex;
            justify-content: space-between;
            align-items: flex-start;
            margin-bottom: 1rem;
        }
        .institution-profile-name {
            font-size: 1.25rem;
            font-weight: 600;
            margin-bottom: 0.25rem;
        }
        .institution-profile-type {
            color: #6c757d;
            font-size: 0.875rem;
            margin-bottom: 0;
        }
        .institution-profile-change {
            font-size: 1.25rem;
            font-weight: 600;
        }
        .institution-profile-stats {
            display: flex;
            margin-top: 1rem;
            flex-wrap: wrap;
        }
        .institution-profile-stat {
            flex: 1;
            min-width: 120px;
            text-align: center;
            padding: 0.5rem;
            border-right: 1px solid #dee2e6;
        }
        .institution-profile-stat:last-child {
            border-right: none;
        }
        .institution-profile-stat-value {
            font-size: 1.25rem;
            font-weight: 600;
            margin-bottom: 0.25rem;
        }
        .institution-profile-stat-label {
            color: #6c757d;
            font-size: 0.75rem;
            margin-bottom: 0;
        }
        .pagination-container {
            display: flex;
            justify-content: center;
            margin-top: 1.5rem;
        }
        .position-progress {
            height: 8px;
            margin-top: 0.5rem;
        }
        .position-progress .progress-bar {
            background-color: #0d6efd;
        }
        .historical-chart {
            height: 300px;
            width: 100%;
        }
        .ownership-gauge {
            position: relative;
            width: 200px;
            height: 200px;
            margin: 0 auto;
        }
        .ownership-gauge-circle {
            width: 200px;
            height: 200px;
            border-radius: 100%;
            background: #e9ecef;
            position: relative;
            overflow: hidden;
        }
        .ownership-gauge-fill {
            position: absolute;
            width: 200px;
            height: 200px;
            background-color: #0d6efd;
            transform-origin: center bottom;
        }
        .ownership-gauge-center {
            position: absolute;
            width: 150px;
            height: 150px;
            background: white;
            border-radius: 100%;
            top: 25px;
            left: 25px;
            z-index: 2;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }
        .ownership-gauge-value {
            font-size: 2.5rem;
            font-weight: bold;
            line-height: 1;
        }
        .ownership-gauge-label {
            font-size: 0.75rem;
            color: #6c757d;
            text-transform: uppercase;
        }
        .distribution-type {
            font-weight: 600;
        }
        .filtering-section {
            margin-bottom: 1rem;
        }
        .filtering-section label {
            font-weight: 600;
            margin-bottom: 0.5rem;
        }
    </style>
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            // Set ownership gauge (semi-circle chart)
            const totalInstitutionalOwnership = {{ totalInstitutionalOwnership|default(0) }};
            const gaugeElement = document.querySelector('.ownership-gauge-fill');
            if (gaugeElement) {
                // Calculate angle for semi-circle
                const angle = (totalInstitutionalOwnership / 100) * 180;
                gaugeElement.style.transform = `rotate(${90 - angle}deg)`;
                gaugeElement.style.transformOrigin = 'center bottom';

                // Set color based on ownership percentage
                let color;
                if (totalInstitutionalOwnership >= 75) {
                    color = '#198754'; // High institutional ownership - green
                } else if (totalInstitutionalOwnership >= 50) {
                    color = '#0d6efd'; // Medium-high institutional ownership - blue
                } else if (totalInstitutionalOwnership >= 25) {
                    color = '#ffc107'; // Medium-low institutional ownership - yellow
                } else {
                    color = '#dc3545'; // Low institutional ownership - red
                }
                gaugeElement.style.backgroundColor = color;
            }

            // Top Holders Chart
            const holdersCtx = document.getElementById('topHoldersChart').getContext('2d');

            // Extract data from the institutional owners section
            const owners = [];
            const percentages = [];
            document.querySelectorAll('.institution-data').forEach(function(el) {
                const name = el.getAttribute('data-name');
                const percentage = parseFloat(el.getAttribute('data-percentage'));
                owners.push(name);
                percentages.push(percentage);
            });

            // Get top 10 holders
            const topOwners = owners.slice(0, 10);
            const topPercentages = percentages.slice(0, 10);

            // Create horizontal bar chart
            new Chart(holdersCtx, {
                type: 'bar',
                data: {
                    labels: topOwners,
                    datasets: [{
                        label: 'Percentage Owned',
                        data: topPercentages,
                        backgroundColor: 'rgba(13, 110, 253, 0.7)',
                        borderColor: 'rgba(13, 110, 253, 1)',
                        borderWidth: 1
                    }]
                },
                options: {
                    responsive: true,
                    maintainAspectRatio: false,
                    indexAxis: 'y',
                    scales: {
                        x: {
                            beginAtZero: true,
                            title: {
                                display: true,
                                text: 'Percentage Owned (%)'
                            },
                            max: Math.max(...topPercentages) * 1.2 // Add some padding
                        }
                    },
                    plugins: {
                        title: {
                            display: true,
                            text: 'Top 10 Institutional Holders'
                        },
                        tooltip: {
                            callbacks: {
                                label: function(context) {
                                    return context.parsed.x.toFixed(2) + '%';
                                }
                            }
                        }
                    }
                }
            });

            // Historical Ownership Chart
            const historyCtx = document.getElementById('ownershipHistoryChart').getContext('2d');

            // Sample data - in production this would come from the ownershipHistory variable
            const quarters = [];
            const institutionalOwnership = [];
            const insiderOwnership = [];
            const retailOwnership = [];

            // Generate sample data for the last 8 quarters
            const currentDate = new Date();
            for (let i = 7; i >= 0; i--) {
                const date = new Date(currentDate);
                date.setMonth(currentDate.getMonth() - (i * 3));
                const quarter = 'Q' + (Math.floor(date.getMonth() / 3) + 1) + ' ' + date.getFullYear();
                quarters.push(quarter);

                // Simulate gradual change in ownership
                const baseInstitutional = {{ totalInstitutionalOwnership|default(50) }};
                const baseInsider = 10;
                const institutionalValue = baseInstitutional + (Math.random() * 10 - 5);
                const insiderValue = baseInsider + (Math.random() * 3 - 1.5);
                const retailValue = 100 - institutionalValue - insiderValue;

                institutionalOwnership.push(institutionalValue);
                insiderOwnership.push(insiderValue);
                retailOwnership.push(retailValue);
            }

            new Chart(historyCtx, {
                type: 'line',
                data: {
                    labels: quarters,
                    datasets: [
                        {
                            label: 'Institutional Ownership',
                            data: institutionalOwnership,
                            borderColor: 'rgba(13, 110, 253, 1)',
                            backgroundColor: 'rgba(13, 110, 253, 0.1)',
                            borderWidth: 2,
                            fill: true,
                            tension: 0.3
                        },
                        {
                            label: 'Insider Ownership',
                            data: insiderOwnership,
                            borderColor: 'rgba(25, 135, 84, 1)',
                            backgroundColor: 'rgba(25, 135, 84, 0.1)',
                            borderWidth: 2,
                            fill: true,
                            tension: 0.3
                        },
                        {
                            label: 'Retail & Other',
                            data: retailOwnership,
                            borderColor: 'rgba(108, 117, 125, 1)',
                            backgroundColor: 'rgba(108, 117, 125, 0.1)',
                            borderWidth: 2,
                            fill: true,
                            tension: 0.3
                        }
                    ]
                },
                options: {
                    responsive: true,
                    maintainAspectRatio: false,
                    scales: {
                        y: {
                            beginAtZero: true,
                            title: {
                                display: true,
                                text: 'Percentage (%)'
                            },
                            stacked: true,
                            max: 100
                        },
                        x: {
                            title: {
                                display: true,
                                text: 'Quarter'
                            }
                        }
                    },
                    plugins: {
                        title: {
                            display: true,
                            text: 'Ownership History'
                        },
                        tooltip: {
                            callbacks: {
                                label: function(context) {
                                    return context.dataset.label + ': ' + context.parsed.y.toFixed(2) + '%';
                                }
                            }
                        }
                    }
                }
            });

            // Institution Type Distribution Chart
            const typeDistributionCtx = document.getElementById('typeDistributionChart').getContext('2d');

            new Chart(typeDistributionCtx, {
                type: 'pie',
                data: {
                    labels: ['Investment Advisor', 'Hedge Fund', 'Pension Fund', 'Bank', 'Insurance Company', 'Other'],
                    datasets: [{
                        data: [45, 25, 12, 8, 6, 4], // Sample data - would be generated from institutionalOwners
                        backgroundColor: [
                            'rgba(13, 110, 253, 0.7)',
                            'rgba(25, 135, 84, 0.7)',
                            'rgba(255, 193, 7, 0.7)',
                            'rgba(220, 53, 69, 0.7)',
                            'rgba(111, 66, 193, 0.7)',
                            'rgba(108, 117, 125, 0.7)'
                        ],
                        borderColor: [
                            'rgba(13, 110, 253, 1)',
                            'rgba(25, 135, 84, 1)',
                            'rgba(255, 193, 7, 1)',
                            'rgba(220, 53, 69, 1)',
                            'rgba(111, 66, 193, 1)',
                            'rgba(108, 117, 125, 1)'
                        ],
                        borderWidth: 1
                    }]
                },
                options: {
                    responsive: true,
                    maintainAspectRatio: false,
                    plugins: {
                        title: {
                            display: true,
                            text: 'Distribution by Institution Type'
                        },
                        tooltip: {
                            callbacks: {
                                label: function(context) {
                                    const label = context.label || '';
                                    const value = context.parsed;
                                    const total = context.dataset.data.reduce((a, b) => a + b, 0);
                                    const percentage = Math.round((value / total) * 100);
                                    return `${label}: ${percentage}%`;
                                }
                            }
                        }
                    }
                }
            });
        });
    </script>
{% endblock %}

{% block body %}
    <div class="container mt-4">
        <h1>{{ company.name }} ({{ company.tickerSymbol }}) - Institutional Ownership</h1>

        <nav aria-label="breadcrumb" class="mb-4">
            <ol class="breadcrumb">
                <li class="breadcrumb-item"><a href="{{ path('company_show', {'id': company.id}) }}">Company Profile</a></li>
                <li class="breadcrumb-item"><a href="{{ path('company_additional_metrics', {'id': company.id}) }}">Additional Metrics</a></li>
                <li class="breadcrumb-item active" aria-current="page">Institutional Ownership</li>
            </ol>
        </nav>

        <div class="metrics-nav">
            <ul class="nav nav-pills">
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('company_additional_metrics', {'id': company.id}) }}">
                        <i class="fas fa-chart-line"></i> Overview
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('company_esg_dashboard', {'id': company.id}) }}">
                        <i class="fas fa-leaf"></i> ESG
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('company_sec_filings', {'id': company.id}) }}">
                        <i class="fas fa-file-alt"></i> SEC Filings
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('company_insider_activity', {'id': company.id}) }}">
                        <i class="fas fa-user-tie"></i> Insider Activity
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link active" href="{{ path('company_institutional_ownership', {'id': company.id}) }}">
                        <i class="fas fa-building"></i> Institutional Ownership
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('company_analyst_coverage', {'id': company.id}) }}">
                        <i class="fas fa-chart-bar"></i> Analyst Coverage
                    </a>
                </li>
            </ul>
        </div>

        <div class="row">
            <!-- Key Figures -->
            <div class="col-md-3">
                <div class="key-figure-card">
                    <div class="key-figure-value">{{ totalInstitutionalOwnership|default(0)|number_format(2) }}%</div>
                    <div class="key-figure-label">Total Institutional<br>Ownership</div>
                </div>
            </div>
            <div class="col-md-3">
                <div class="key-figure-card">
                    <div class="key-figure-value">{{ institutionalOwners|length }}</div>
                    <div class="key-figure-label">Institutional<br>Investors</div>
                </div>
            </div>
            <div class="col-md-3">
                <div class="key-figure-card">
                    {% set netChange = 0 %}
                    {% for owner in institutionalOwners %}
                        {% set netChange = netChange + owner.percentageChange %}
                    {% endfor %}
                    <div class="key-figure-value {% if netChange > 0 %}change-positive{% elseif netChange < 0 %}change-negative{% else %}change-neutral{% endif %}">
                        {% if netChange > 0 %}+{% endif %}{{ netChange|number_format(2) }}%
                    </div>
                    <div class="key-figure-label">Net Change<br>Last Quarter</div>
                </div>
            </div>
            <div class="col-md-3">
                <div class="key-figure-card">
                    {% set buyersCount = 0 %}
                    {% set sellersCount = 0 %}
                    {% for owner in institutionalOwners %}
                        {% if owner.percentageChange > 0 %}
                            {% set buyersCount = buyersCount + 1 %}
                        {% elseif owner.percentageChange < 0 %}
                            {% set sellersCount = sellersCount + 1 %}
                        {% endif %}
                    {% endfor %}
                    <div class="key-figure-value">{{ buyersCount }} / {{ sellersCount }}</div>
                    <div class="key-figure-label">Buyers / Sellers<br>Last Quarter</div>
                </div>
            </div>
        </div>

        <div class="row mt-4">
            <!-- Ownership Gauge -->
            <div class="col-md-4">
                <div class="card metrics-card">
                    <div class="card-header">
                        Total Institutional Ownership
                    </div>
                    <div class="card-body">
                        <div class="ownership-gauge">
                            <div class="ownership-gauge-circle">
                                <div class="ownership-gauge-fill"></div>
                            </div>
                            <div class="ownership-gauge-center">
                                <div class="ownership-gauge-value">{{ totalInstitutionalOwnership|default(0)|number_format(2) }}%</div>
                                <div class="ownership-gauge-label">Institutional</div>
                            </div>
                        </div>

                        <div class="d-flex justify-content-between mt-4">
                            <div class="text-center">
                                <div>{{ insiderOwnership|default(10)|number_format(2) }}%</div>
                                <small class="text-muted">Insider</small>
                            </div>
                            <div class="text-center">
                                <div>{{ (100 - totalInstitutionalOwnership|default(0) - insiderOwnership|default(10))|number_format(2) }}%</div>
                                <small class="text-muted">Retail & Other</small>
                            </div>
                        </div>

                        <div class="mt-4">
                            <p class="mb-2">Institutional ownership represents the percentage of a company's shares held by institutional investors such as mutual funds, pension funds, and hedge funds.</p>
                            <p class="mb-0">
                                <strong>Last updated:</strong> {{ 'now'|date('M d, Y') }}
                            </p>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Top Holders Chart -->
            <div class="col-md-8">
                <div class="card metrics-card">
                    <div class="card-header">
                        Top Institutional Holders
                    </div>
                    <div class="card-body">
                        <div class="ownership-chart">
                            <canvas id="topHoldersChart"></canvas>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <div class="row mt-4">
            <!-- Historical Ownership -->
            <div class="col-md-8">
                <div class="card metrics-card">
                    <div class="card-header">
                        Historical Ownership Trends
                    </div>
                    <div class="card-body">
                        <div class="historical-chart">
                            <canvas id="ownershipHistoryChart"></canvas>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Institution Type Distribution -->
            <div class="col-md-4">
                <div class="card metrics-card">
                    <div class="card-header">
                        Ownership by Institution Type
                    </div>
                    <div class="card-body">
                        <div class="ownership-chart">
                            <canvas id="typeDistributionChart"></canvas>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <div class="row mt-4">
            <div class="col-12">
                <div class="card metrics-card">
                    <div class="card-header">
                        Institutional Holders
                    </div>
                    <div class="card-body">
                        <!-- Filters -->
                        <div class="filtering-section">
                            <form method="get" action="{{ path('company_institutional_ownership', {'id': company.id}) }}" class="row g-3">
                                <div class="col-md-4">
                                    <label for="sort">Sort By</label>
                                    <select id="sort" name="sort" class="form-select">
                                        <option value="shares" {% if currentSort == 'shares' %}selected{% endif %}>Shares Held</option>
                                        <option value="percentage" {% if currentSort == 'percentage' %}selected{% endif %}>Percentage Owned</option>
                                        <option value="value" {% if currentSort == 'value' %}selected{% endif %}>Value</option>
                                        <option value="change" {% if currentSort == 'change' %}selected{% endif %}>Recent Change</option>
                                    </select>
                                </div>
                                <div class="col-md-4">
                                    <label for="type">Institution Type</label>
                                    <select id="type" name="type" class="form-select">
                                        <option value="">All Types</option>
                                        {% for type in institutionTypes %}
                                            <option value="{{ type }}" {% if currentType == type %}selected{% endif %}>{{ type }}</option>
                                        {% endfor %}
                                    </select>
                                </div>
                                <div class="col-md-4 d-flex align-items-end">
                                    <button type="submit" class="btn btn-primary">Apply Filters</button>
                                    {% if currentSort or currentType %}
                                        <a href="{{ path('company_institutional_ownership', {'id': company.id}) }}" class="btn btn-outline-secondary ms-2">Clear Filters</a>
                                    {% endif %}
                                </div>
                            </form>
                        </div>

                        {% if institutionalOwners is not empty %}
                            <div class="table-responsive">
                                <table class="table table-hover">
                                    <thead>
                                        <tr>
                                            <th>Institution</th>
                                            <th>Type</th>
                                            <th>Shares</th>
                                            <th>Value</th>
                                            <th>% Owned</th>
                                            <th>% Change</th>
                                            <th>Date Reported</th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        {% for owner in institutionalOwners %}
                                            <tr class="institution-data" data-name="{{ owner.institutionName }}" data-percentage="{{ owner.percentageOwned }}">
                                                <td class="institution-name">{{ owner.institutionName }}</td>
                                                <td>{{ owner.institutionType|default('Investment Advisor') }}</td>
                                                <td>{{ owner.shares|number_format }}</td>
                                                <td>${{ owner.value|number_format }}</td>
                                                <td>{{ owner.percentageOwned|number_format(2) }}%</td>
                                                <td>
                                                    {% if owner.percentageChange > 0 %}
                                                        <span class="change-positive">+{{ owner.percentageChange|number_format(2) }}%</span>
                                                    {% elseif owner.percentageChange < 0 %}
                                                        <span class="change-negative">{{ owner.percentageChange|number_format(2) }}%</span>
                                                    {% else %}
                                                        <span class="change-neutral">{{ owner.percentageChange|number_format(2) }}%</span>
                                                    {% endif %}
                                                </td>
                                                <td>{{ owner.reportDate|date('M d, Y') }}</td>
                                            </tr>
                                        {% endfor %}
                                    </tbody>
                                </table>
                            </div>

                            <div class="pagination-container">
                                <nav aria-label="Institutional owners pagination">
                                    <ul class="pagination">
                                        <li class="page-item disabled">
                                            <a class="page-link" href="#" tabindex="-1" aria-disabled="true">Previous</a>
                                        </li>
                                        <li class="page-item active" aria-current="page">
                                            <a class="page-link" href="#">1</a>
                                        </li>
                                        <li class="page-item">
                                            <a class="page-link" href="#">2</a>
                                        </li>
                                        <li class="page-item">
                                            <a class="page-link" href="#">3</a>
                                        </li>
                                        <li class="page-item">
                                            <a class="page-link" href="#">Next</a>
                                        </li>
                                    </ul>
                                </nav>
                            </div>
                        {% else %}
                            <div class="alert alert-info">
                                <p class="mb-0">No institutional ownership data found for {{ company.name }}{% if currentType %} with the selected filters{% endif %}.</p>
                            </div>
                        {% endif %}
                    </div>
                </div>
            </div>
        </div>

        <div class="row mt-4">
            <div class="col-12">
                <div class="card metrics-card">
                    <div class="card-header">
                        Understanding Institutional Ownership
                    </div>
                    <div class="card-body">
                        <h5>What is Institutional Ownership?</h5>
                        <p>
                            Institutional ownership refers to the ownership stake in a company that is held by large financial institutions such as:
                        </p>
                        <div class="row">
                            <div class="col-md-4">
                                <ul>
                                    <li><span class="distribution-type">Investment Advisors</span> - Asset management firms that oversee investments for clients</li>
                                    <li><span class="distribution-type">Hedge Funds</span> - Private investment partnerships with sophisticated strategies</li>
                                </ul>
                            </div>
                            <div class="col-md-4">
                                <ul>
                                    <li><span class="distribution-type">Pension Funds</span> - Retirement funds for employees</li>
                                    <li><span class="distribution-type">Mutual Funds</span> - Pooled investment vehicles managed professionally</li>
                                </ul>
                            </div>
                            <div class="col-md-4">
                                <ul>
                                    <li><span class="distribution-type">Banks</span> - Commercial and investment banks</li>
                                    <li><span class="distribution-type">Insurance Companies</span> - Companies managing insurance policy premiums</li>
                                </ul>
                            </div>
                        </div>

                        <h5 class="mt-4">Why Institutional Ownership Matters</h5>
                        <p>
                            Institutional ownership provides valuable insights for investors for several reasons:
                        </p>
                        <div class="row">
                            <div class="col-md-6">
                                <ul>
                                    <li><strong>Investment Quality Signal:</strong> High institutional ownership can indicate professional investors see value in the company</li>
                                    <li><strong>Market Stability:</strong> Institutions typically have longer investment horizons than retail investors</li>
                                    <li><strong>Corporate Governance:</strong> Large institutional investors can influence company decisions and governance practices</li>
                                </ul>
                            </div>
                            <div class="col-md-6">
                                <ul>
                                    <li><strong>Market Liquidity:</strong> Institutional trading provides liquidity, making it easier for all investors to buy and sell shares</li>
                                    <li><strong>Research Access:</strong> Institutions with large positions often conduct deep research on companies</li>
                                    <li><strong>Price Impact:</strong> Significant institutional buying or selling can impact stock prices</li>
                                </ul>
                            </div>
                        </div>

                        <h5 class="mt-4">How to Interpret Changes in Institutional Ownership</h5>
                        <div class="row">
                            <div class="col-md-6">
                                <p><strong>Increasing Institutional Ownership</strong></p>
                                <ul>
                                    <li>Could signal growing confidence in the company's prospects</li>
                                    <li>May result from inclusion in major indices (S&P 500, Russell 2000, etc.)</li>
                                    <li>Sometimes follows improved financial performance or strategic changes</li>
                                </ul>
                            </div>
                            <div class="col-md-6">
                                <p><strong>Decreasing Institutional Ownership</strong></p>
                                <ul>
                                    <li>Might indicate concerns about the company's future</li>
                                    <li>Could result from portfolio rebalancing or strategy changes</li>
                                    <li>Sometimes occurs due to broader market shifts rather than company-specific issues</li>
                                </ul>
                            </div>
                        </div>

                        <div class="alert alert-info mt-3">
                            <h6 class="alert-heading"><i class="fas fa-info-circle me-2"></i>Institutional Filing Requirements</h6>
                            <p class="mb-0">Institutional investors with over $100 million in assets must file quarterly 13F reports with the SEC disclosing their equity holdings. Additionally, investors acquiring 5% or more of a company's shares must file Schedule 13D or 13G forms. These filings provide the data shown on this page.</p>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
